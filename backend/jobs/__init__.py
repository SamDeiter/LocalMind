"""
LocalMind Jobs — Autonomous task execution pipeline.

The "train" system: planner (engine) -> node executor (cars) -> reviewer (caboose).
Supports quick mode (auto-planned) and pipeline mode (user-defined templates).
"""
